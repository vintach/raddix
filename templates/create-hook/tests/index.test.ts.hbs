import { renderHook, act } from '@testing-library/react';
import { use{{pascalCase hookName}} } from '../src';

describe('use{{pascalCase hookName}} test:', () => {
  it('should be defined', () => {
    expect(0).toBe(0);
  });
});
